spring:
  datasource:
    url: jdbc:h2:mem:manbanjalbu_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  security:
    user:
      name: test
      password: test

jwt:
  secret: test-secret-key-min-256-bits-long-for-unit-test!!
  expiration-ms: 3600000

member:
  verification:
    expose-dev-code: true

mail:
  verification:
    mock-enabled: true

octomo:
  mock-enabled: true
